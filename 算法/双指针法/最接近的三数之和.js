function threeSumCloest(nums, target){
    let cloestNum = 0;

    for (let i = 0; i < nums.length -2; i++){
        left = i + 1;
        right = nums.length - 1;

        while (left < right){
            trySum = nums[i] + nums[left] + nums[right];
            diff = target - diff;
            if ()
        }
    }
}